import Https from '../utils/https';

export default class File {

    /**
     * 上传项目组头像
     */
    static uploadTeamAvatar() {
        return new Promise((resolve, reject) => {
            
        })
    }

}