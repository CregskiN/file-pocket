<!-- miniprogram/pages/found.wxml -->
<view class="found_container">
    <c-theader avatarUrl="{{userInfo.avatarUrl}}"/>

    <view class="found_body">
        <c-input title="口袋名称" userInfo="{{userInfo}}" bind:finish="onFinish"/>
    </view>

    <view class="found_buttons">
        <view class="found_btn found_btn_left" bind:tap="onBack">
            <text class="iconfont icon-fanhui icon" />
            返回
        </view>
        <view class="found_btn found_btn_right" bind:tap="onComplete">
            <text class="iconfont icon-wancheng icon" />
            完成
        </view>
    </view>
</view>