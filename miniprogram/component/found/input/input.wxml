<!-- component/found/input/input.wxml -->
<view class="input_container">
    <view class="input_title" wx:if="{{title}}">{{title}}</view>
    <input class="input" type="text" placeholder="{{userInfo.nickName + '的口袋'}}"
        bind:input="onChange" bind:blur="onFinish"
    />

</view>